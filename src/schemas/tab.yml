Tab:
  allOf:
    - $ref: './widget.yml#/Widget'
    - type: object
      properties:
        tabDisplayName:
          description: A human readable, non-unique name to be displayed
          type: string
        widgets: 
          type: array
          items: 
            type: string
      required:
        - tabDisplayName
        - widgets
