Page:
  type: object
  properties:
    id: 
      type: string
    widgetIds: 
      type: array
      items: 
        type: string
    route:
      description: A URL-safe route where this page can be accessed
      type: string
  required:
    - widgetIds
    - route
