Page:
  type: object
  properties:
    widgets:
      type: array
      items:
        $ref: './widget.yml#/Widget'
    route:
      description: A URL-safe route where this page can be accessed
      type: string
