Page:
  type: object
  properties:
    id: 
      type: string
    widgets: 
      type: array
      items: 
        type: string
    route:
      description: A URL-safe route where this page can be accessed
      type: string
  required:
    - widgets
    - route
