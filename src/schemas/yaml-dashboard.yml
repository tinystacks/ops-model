---
openapi: 3.0.0
info:
  title: todo
  description: todo
  version: 1.0.120
paths: {}
components:
  schemas:
    YamlDashboard:
      type: object
      properties:
        route:
          type: string
        widgets:
          type: array
          items:
            $ref: yaml-widget.yml#/components/schemas/YamlWidget
        parameters:
          type: array
          items:
            $ref: parameter.yml#/components/schemas/Parameter
      required:
        - route